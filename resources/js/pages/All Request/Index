<script setup lang="ts">
import AppLayout from '@/layouts/AppLayout.vue';
import { Head, Link, router } from '@inertiajs/vue3';
import { type breadcrumbs } from '@inertiajs/vue3';

const breadcrumbs = [
    { title: 'Dashboard',
     href: '/dashboard'
    },

    { title: 'All Requests',
    href: '/certificate-requests' 
    },

];
</script> 

<template>
    <Head title="All Requests" />
    <AppLayout :breadcrumbs="breadcrumbs">
        <div class="flex items-center justify-between mb-4">
            <h1 class="text-2xl font-bold">All Requests</h1>
        </div>
        <div class="overflow-hidden rounded-lg border bg-card shadow-md dark:border-slate-700 dark:bg-slate-800">
            <table class="w-full table-auto text-left text-sm text-gray-500 dark:text-gray-400">
                <thead class="bg-gray-50 text-xs uppercase text-gray-700 dark:bg-gray-700 dark:text-gray-400">
                    <tr>
                        <th class="px-6 py-3">ID</th>
                        <th class="px-6 py-3">User</th>
                        <th class="px-6 py-3">Type</th>
                        <th class="px-6 py-3">Status</th>
                        <th class="px-6 py-3">Requested At</th>
                    </tr>
                </thead>
                <tbody>
                    <tr v-for="request in requests" :key="request.id" class="border-b bg-white dark:border-gray-700 dark:bg-gray-800">
                        <td class="px-6 py-4">{{ request.id }}</td>
                        <td class="px-6 py-4">{{ request.user?.name || 'N/A' }}</td>
                        <td class="px-6 py-4">{{ request.type }}</td>
                        <td class="px-6 py-4">
                            <span v-if="request.status === 'approved'" class="text-green-600">Approved</span>
                            <span v-else-if="request.status === 'pending'" class="text-yellow-600">Pending</span>
                            <span v-else class="text-red-600">Rejected</span>
                        </td>
                        <td class="px-6 py-4">{{ request.created_at }}</td>
                        <td class="px-6 py-4">
                            <button class="text-blue-600 hover:text-blue-900">View</button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </AppLayout>
</template>